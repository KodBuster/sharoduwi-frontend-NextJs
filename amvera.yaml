# Amvera — Docker + Next.js standalone (не npm run dev!)
# В UI Amvera: Конфигурация → команда запуска должна быть пустой или из этого файла.
# Переменные окружения — в панели (см. .env.example)

meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 80
